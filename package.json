{
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:frontend": "npm --prefix frontend run dev",
    "ollama": "cmd /c start \"Ollama Server\" powershell -NoExit -Command \"ollama serve\"",
    "dev:with-ollama-window": "npm run ollama && npm run dev",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && python -m pytest -q",
    "test:frontend": "npm --prefix frontend test",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "concurrently": "^9.2.1"
  }
}
